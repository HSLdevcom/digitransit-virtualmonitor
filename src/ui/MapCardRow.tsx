import React, { FunctionComponent } from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface IProps {}

const mapCardRow: FunctionComponent<IProps> = (props: IProps) => {
  return <div className="map-card">Placeholder Map Card Row Component</div>;
};

export default mapCardRow;
